<h2>Tick out the final eligible ones(after interview)</h2>
<form method="post" action="/admins/superAdmin/finalList">
   
<table>
    <tr>
        <th></th>
        <th>Registration ID</th>
        <th>Gender</th>
        <th>Post Applied</th>
        <th>Name</th>
        <th></th>
        <th></th>
    </tr>
    
    <% docs.forEach((doc) => { %>
    <tr>
        <td><input type="checkbox" name="<%= doc.userId %>" value="true" /></td>
        <td><%= doc.userId %></td>
        <td><%= doc.gender %></td>
        <td><%= doc.postApplied %></td>
        <td><%= doc.name %></td>
        <td><textarea name="<%= doc._id %>" placeholder="Remarks"></textarea></td>
        <td><a target="_blank" href="/admins/userdetail/<%= doc._id %>">view</a></td>
    </tr>
        <% }) %>
</table>
    <br><br>
    <p>Send the final list to the establishment section</p>
    <button type="submit">Submit</button>
</form>
<a href="/admins/logout" class="btn btn-secondary">Logout</a>
